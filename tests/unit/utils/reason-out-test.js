import reasonOut from 'chaibase/utils/reason-out';
import { module, test } from 'qunit';

module('Unit | Utility | reason out');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reasonOut();
  assert.ok(result);
});
