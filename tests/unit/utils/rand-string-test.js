import randString from 'chaibase/utils/rand-string';
import { module, test } from 'qunit';

module('Unit | Utility | rand string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = randString();
  assert.ok(result);
});
