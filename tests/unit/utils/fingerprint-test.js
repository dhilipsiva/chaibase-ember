import fingerprint from 'chaibase/utils/fingerprint';
import { module, test } from 'qunit';

module('Unit | Utility | fingerprint');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fingerprint();
  assert.ok(result);
});
